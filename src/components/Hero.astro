---
// Hero section component
---

<section class="hero">
  <div class="hero-content">
    <h1>JTAPS<br /><span>BAR AND GRILL</span></h1>
    <p class="hero-tagline">
      Whether You're Here For The Big Game, A Casual Night Out, Or To Dig Into The Best Gyros And Wings In Town With A Cold Beer In Hand, Jtaps Bar And Grill Has You Covered. Swing By And Experience The Ultimate Sports Bar Vibe—Where Great Food, Epic Games, And Good Times Collide!
    </p>
    <a href="https://togoorder.com/web/3136#/" target="_blank" class="cta-button">ORDER NOW</a>
  </div>
</section>

<style>
  @import url('https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Righteous&display=swap');

  .hero {
    background: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.5)), url('/images/6441-Glenway.jpg') center/cover;
    min-height: 70vh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    color: var(--text-light);
    padding: 2rem;
  }

  .hero-content {
    max-width: 900px;
  }

  .hero h1 {
    font-size: 5rem;
    font-weight: 900;
    margin-bottom: 1rem;
    color: var(--secondary-color);
    font-family: 'Bebas Neue', cursive;
    letter-spacing: 3px;
    text-transform: uppercase;
    text-shadow: 3px 3px 6px rgba(0,0,0,0.8);
  }

  .hero h1 span {
    font-size: 4rem;
    display: block;
    font-family: 'Righteous', cursive;
    letter-spacing: 2px;
  }

  .hero-tagline {
    font-size: 1.2rem;
    margin-bottom: 2rem;
    line-height: 1.8;
    font-weight: 500;
  }

  .cta-button {
    display: inline-block;
    background: var(--secondary-color);
    color: var(--primary-color);
    padding: 1rem 3rem;
    text-decoration: none;
    font-weight: bold;
    border-radius: 5px;
    transition: transform 0.3s, box-shadow 0.3s;
  }

  .cta-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(212, 175, 55, 0.3);
  }

  @media (max-width: 1024px) {
    .hero h1 {
      font-size: 4rem;
    }

    .hero h1 span {
      font-size: 3rem;
    }
  }

  @media (max-width: 768px) {
    .hero {
      min-height: 60vh;
      padding: 1.5rem;
    }

    .hero h1 {
      font-size: 2.5rem;
    }

    .hero h1 span {
      font-size: 2rem;
    }

    .hero-tagline {
      font-size: 1rem;
      line-height: 1.6;
    }

    .cta-button {
      padding: 0.8rem 2rem;
      font-size: 0.95rem;
    }
  }

  @media (max-width: 480px) {
    .hero {
      min-height: 50vh;
      padding: 1rem;
    }

    .hero h1 {
      font-size: 2rem;
    }

    .hero h1 span {
      font-size: 1.5rem;
    }

    .hero-tagline {
      font-size: 0.9rem;
    }

    .cta-button {
      padding: 0.75rem 1.5rem;
      min-height: 44px;
    }
  }
</style>
